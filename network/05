# Identify the IP address of the DNS that responds to the following url: slash16.org

nslookup slash16.org | awk '/Server/{print $2}'


# nslookup -> name server lookup
#			  is a network administration command-line tool available for many computer operating systems
#			  for querying the Domain Name System (DNS) to obtain domain name or IP address mapping or for
#   		  any other specific DNS record.



# dig slash16.org
